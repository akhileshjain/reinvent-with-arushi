/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      // Real client photography will be added here once supplied.
      // Until then, all imagery is rendered via the local PlaceholderImage component.
    ],
  },
};

module.exports = nextConfig;
